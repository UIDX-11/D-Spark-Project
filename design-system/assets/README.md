# Assets (入口层，不存真源)

此目录用于放**入口链接**（for humans / for agents），不复制真源文件，避免产生多份“看起来都像真源”的内容。

## Design tokens (CSS dist)

- `.design-spec/tokens/dist/tokens.css`

> 说明：token 的结构化真源在 `.design-spec/tokens/src/`（core → semantic → component）。
