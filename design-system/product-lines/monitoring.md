# Product line module: Monitoring

Load when the request involves:

- metrics / dashboard / chart
- logs / tracing
- alerts / incidents

## Module-specific rules

- Default views must show “what changed” quickly (trend + current value + time range).
- Time range controls must be consistent across pages.
- Alert severity must be explicit with text + icon semantics (not color only).

