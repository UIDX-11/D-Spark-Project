# Flutter Adapters

This folder describes how the design spec and tokens map to your Flutter design system.

## Adapter responsibilities

- Map semantic/component tokens to Flutter theme primitives (colors, text styles, radii).
- Ensure controlled widgets expose only token-driven styling.
- Document the “preferred composition patterns” for B-end-like Flutter screens (forms, tables/lists, detail pages).

