# 品牌图标资产

- **Figma 入口**：`fileKey` `KJfy0GFDs8kLsXTzhTxAjd`，节点 **`276593:77`**（图标库根）。
- **导出**：SVG；设计网格 **48×48**；HTML 默认展示 **`--semantic-icon-size-md`（16）**，见 `tokens/dist/tokens.css`。
- **manifest**：同目录 `icons.manifest.json`（`id`、`relativePath`、`figmaNodeId`、`export` 约定见文件内字段）。

需求与阶段计划：[`../docs/REQUIREMENTS_AND_PLAN.md`](../docs/REQUIREMENTS_AND_PLAN.md)。

缺失图标时的分级处理见 `.cursor/skills/design-spec-hybrid-spec/SKILL.md`（或 `docs/foundations/` 中图标治理段落）。
