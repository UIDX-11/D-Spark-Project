# Figma 组件命名审查表（模板）

**约束**：只读拉取 + 书面输出；**禁止**未获产品/设计确认前在 Figma 内批量 Rename。

## 元信息

| 字段 | 值 |
|------|-----|
| 审查日期 | |
| 审查人 | |
| 官方库 fileKey | KJfy0GFDs8kLsXTzhTxAjd |
| 参考组件 ID（slug） | |

## 审查表

| Figma node-id | 画布上组件名 / COMPONENT_SET | 变体 Property | 枚举值示例 | 与 MD/代码命名一致性 | 问题说明 | 建议名（可选） | 状态 |
|---------------|------------------------------|---------------|------------|----------------------|----------|----------------|------|
| | | | | | | | 待确认 / 已采纳 / 不修改 |

## 说明

- 使用 Figma MCP `get_metadata` / `get_design_context` 或设计工具导出清单填充上表。  
- 「建议名」仅作提案；在 Figma 执行 Rename 前须单独签字确认。
