# Figma 图标名 ↔ Arco Icon（可选降级）

当 **非 1:1 对稿** demo 且 manifest 无 SVG 时，可查阅此表；**规范对稿仍以品牌 SVG + manifest 为准**。


| Figma / 语义 | Arco Design Web React 组件 / 说明 | 备注                                |
| ---------- | ---------------- | --------------------------------- |
| （待设计填写）    | —                | 使用 `@arco-design/web-react/icon` |


维护：与 `.design-spec/assets/icons/icons.manifest.json` 同步迭代。